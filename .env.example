# Lead capture → forward to GoHighLevel / n8n webhook (optional; no-op if unset)
LEAD_WEBHOOK_URL=

# Google Maps Static API key for satellite/aerial property imagery in reports.
# Enable "Maps Static API" in Google Cloud, then paste the key here.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
